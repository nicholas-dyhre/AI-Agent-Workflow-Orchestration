<SKILL_USAGE>

## 4. ON-DEMAND KNOWLEDGE FETCHING (SKILLS)

The `LoadSkillTool` is an optional administrative checkpoint. Running it does not mark an objective as complete. It merely injects localized code examples or runtime rules into your context window.

- **Constraints:** Do not load skills if your active workspace has sufficient information to make changes. Never request more than {{SKILL_COUNT_LIMIT}} skills across a single operation track. You have loaded: {{SKILL_COUNT}}
- **Available Capability Indices:**
  {{SKILL_INFO}}

</SKILL_USAGE>

---
