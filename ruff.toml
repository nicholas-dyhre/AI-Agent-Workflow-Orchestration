target-version = "py311"
line-length = 100

[lint]
# Enable standard pycodestyle (E), Pyflakes (F), and isort (I) rules
select = ["E", "F", "I", "W"]
fixable = ["ALL"]

[format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false